/**
 * The last of the mock-era shapes. Everything else moved to the generated
 * OpenAPI types in `src/shared/api` — this only survives because the i18n
 * content bundle types its emoji-plus-label lists with it.
 */
export interface Mood {
  emoji: string
  label: string
}
