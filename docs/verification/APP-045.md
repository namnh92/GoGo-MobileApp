# APP-045 — decision-mode flow

Refs #198. Matching routes vote/match rooms through the existing ballot; host mode goes to candidate selection. Saving a ballot waits for API success, keeps failed cards available, and prevents overlapping taps. Host mode finalizes the selected candidate; vote mode sends no explicit override and requires votes; match mode waits for server-created consensus plans. Stale runs cannot be finalized. Known conflicts explain the missing prerequisite, and the result screen links back to the lobby.

Vote/finalize transport retries retain intent keys. Ranking preview follows tally points then candidate rank; it cannot choose an unvoted zero-point candidate over one with only no votes.

Validation: typecheck/lint and 274 unit tests pass (39 skipped). 13 flow tests plus 8 optimistic rollback tests pass, covering vote/host/match routing, no votes, ties, host candidate choice, member restriction, stale runs, failed save, successful advance and double finalize tap. Jest reports the existing open-handle warning after the combined component suites. API host authorization is unchanged; physical-device host/member/guest acceptance remains pending.

Rollback: revert this PR. No contract or schema change.
