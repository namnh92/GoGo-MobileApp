# APP-047 — continue with incomplete preferences

Refs #200. Requires GoGo-BE#565 (OpenAPI alpha.30, ADR-0024).

The lobby uses server-provided matching capabilities. The host can explicitly confirm starting with current responses when at least two members completed, without removing unfinished members or changing party size/budget. Members cannot see this action. Below quorum the lobby explains the requirement. Older APIs retain the existing all-complete path. A synchronous guard prevents duplicate start actions.

Validation: typecheck, lint and generated contract check pass; 274 unit tests pass (39 skipped), and 14 lobby tests pass, including capability gating, host acknowledgement and quorum copy. Deploy BE support before this consumer. Device acceptance remains pending; do not close the behavioral issue on merge.
